node_modules
dist
*.log

# Generated output. The budget and the intent records are committed on purpose:
# one is the CI baseline, the other is the human contribution.
.instantiate/report.html
.instantiate/cache

# Rendered previews, produced while checking the report by eye.
.instantiate/preview*.png

# Calibration pairs labelled on private code.
scripts/labelled-pairs.local.json
